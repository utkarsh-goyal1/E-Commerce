<%- include('../partials/header') %>
    <%- include('../partials/navbar') %>


        <div class="row margin-form">
            <div class="col-6 mx-auto" style="margin-top: 25px;">
                <!-- novalidate is used to remove default validation like we use required -->
                <form action="/products" method="POST" class="needs-validation" novalidate>
                    <div class="mb-3">
                        <label for="naam" class="form-label">Name: </label>
                        <input type="text" class="form-control" name="name" id="naam" placeholder="Name of Product" required>
                        <!-- looks good validator -->
                        <div class="valid-feedback">
                            Looks good!
                        </div>
                        <!-- error validator -->
                        <div class="invalid-feedback">
                            Please choose a productname.
                        </div>                    
                    </div>
                    <div class="mb-3">
                        <label for="imge" class="form-label">Image Url: </label>
                        <input type="text" class="form-control" name="img" id="imge" placeholder="Image URL of Product" required>
                        <!-- looks good validator -->
                        <div class="valid-feedback">
                            Looks good!
                        </div>
                        <!-- error validator -->
                        <div class="invalid-feedback">
                            Please choose a imageurl.
                        </div> 
                    </div>
                    <div class="mb-3">
                        <label for="paisa" class="form-label">Price: </label>
                        <div class="input-group mb-3">
                            <span class="input-group-text" id="basic-addon1">Rs. </span>
                            <input class="form-control" type="number" name="price" id="paisa"
                                placeholder="Price of Product" required>
                                <!-- looks good validator -->
                        <div class="valid-feedback">
                            Looks good!
                        </div>
                        <!-- error validator -->
                        <div class="invalid-feedback">
                            Please choose price.
                        </div> 
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="des" class="form-label">Description: </label>
                        <textarea class="form-control" name="desc" id="des" rows="5"
                            placeholder="Description of Product" required></textarea>
                            <!-- looks good validator -->
                        <div class="valid-feedback">
                            Looks good!
                        </div>
                        <!-- error validator -->
                        <div class="invalid-feedback">
                            Please choose description.
                        </div> 
                    </div>
                    <button type="submit" class="btn btn-sm btn-success">Add</button>
                </form>
            </div>
        </div>
        <script> 
            // Example starter JavaScript for disabling form submissions if there are invalid fields
            (() => {        // Immediately invoked functional expressions(iife) .When the script runs then they immediately runs.
            'use strict'

            // Fetch all the forms we want to apply custom Bootstrap validation styles to
            const forms = document.querySelectorAll('.needs-validation')

            // Loop over them and prevent submission
            Array.from(forms).forEach(form => {
                form.addEventListener('submit', event => {
                if (!form.checkValidity()) {
                    event.preventDefault()
                    event.stopPropagation()
                }

                form.classList.add('was-validated')
                }, false)
            })
            })()//calling function without any name  // Immediately invoked functional expressions(iife)
        </script>

        <%- include('../partials/footer') %>

<!-- 
//required in input field is used for client side validation. When client not fill any field in the form then the form will not submit.But required 
// is handled by our browser.So here we used bootstrap validation.
//The novalidate attribute prevents forms from running their validation logic when the form is submitted. (new.ejs) -->



<!-- server side validation remaining -->

<!-- for server side validation we use a package named "JOI" which is schema descriptive language.It is data validator -->
<!-- It works in two steps-> create a different  schema (schema.js)for this and second step is validate which is a method (defined in middleware.js)-->